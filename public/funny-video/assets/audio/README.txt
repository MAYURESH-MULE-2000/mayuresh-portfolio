Drop your background tracks here as audio1.mp3 ... audio5.mp3 (optional).

When a file is missing, the matching track is synthesised with the Web Audio
API (see js/audio.js). Synthesised tracks are real audio and are recorded into
the exported video exactly like an MP3 would be.
