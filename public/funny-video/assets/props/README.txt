Drop transparent PNG/WebP props here (optional).

Expected names:
  grass.png, flowers.png, lamp.png, heart.png, star.png, balloons.png,
  tree.png, bike.png, car.png, moon.png, cloud.png, wedding.png,
  confetti.png, sparkle.png

When a file is missing, the prop's canvas painter (js/props.js) is used
instead, so the app works without any of these files.
